/** When false, orders stay with JIRIB — no auto-assignment to print partners. */
export const PARTNER_DISPATCH_ENABLED = false;

export function isPartnerDispatchEnabled() {
  return PARTNER_DISPATCH_ENABLED;
}
